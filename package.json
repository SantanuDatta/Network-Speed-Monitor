{
  "name": "network-speed-monitor-firefox",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && cp manifest.json src/popup.html src/dashboard.html src/style.css dist/ && cp -R assets _locales dist/",
    "build-for-amo": "npm run build",
    "dev": "npm run build && web-ext run --source-dir dist --firefox /usr/bin/firefox --start-url 'about:debugging#/runtime/this-firefox'",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run format:check && npm run lint && npm run test && npm run build",
    "package": "npm run build && web-ext build --source-dir dist --artifacts-dir web-ext-artifacts --overwrite-dest"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^6.0.0",
    "web-ext": "^10.0.0"
  }
}
